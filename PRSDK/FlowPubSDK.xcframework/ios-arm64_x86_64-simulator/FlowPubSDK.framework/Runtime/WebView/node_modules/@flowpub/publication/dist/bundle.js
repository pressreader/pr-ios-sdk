(function (global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports) :
  typeof define === 'function' && define.amd ? define(['exports'], factory) :
  (global = global || self, factory(global.flowpub_publication = {}));
}(this, (function (exports) { 'use strict';

  function _typeof(obj) {
    "@babel/helpers - typeof";

    return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) {
      return typeof obj;
    } : function (obj) {
      return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
    }, _typeof(obj);
  }

  function __awaiter(thisArg, _arguments, P, generator) {
    function adopt(value) {
      return value instanceof P ? value : new P(function (resolve) {
        resolve(value);
      });
    }

    return new (P || (P = Promise))(function (resolve, reject) {
      function fulfilled(value) {
        try {
          step(generator.next(value));
        } catch (e) {
          reject(e);
        }
      }

      function rejected(value) {
        try {
          step(generator["throw"](value));
        } catch (e) {
          reject(e);
        }
      }

      function step(result) {
        result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
      }

      step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
  }
  function __generator(thisArg, body) {
    var _ = {
      label: 0,
      sent: function sent() {
        if (t[0] & 1) throw t[1];
        return t[1];
      },
      trys: [],
      ops: []
    },
        f,
        y,
        t,
        g;
    return g = {
      next: verb(0),
      "throw": verb(1),
      "return": verb(2)
    }, typeof Symbol === "function" && (g[Symbol.iterator] = function () {
      return this;
    }), g;

    function verb(n) {
      return function (v) {
        return step([n, v]);
      };
    }

    function step(op) {
      if (f) throw new TypeError("Generator is already executing.");

      while (_) {
        try {
          if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
          if (y = 0, t) op = [op[0] & 2, t.value];

          switch (op[0]) {
            case 0:
            case 1:
              t = op;
              break;

            case 4:
              _.label++;
              return {
                value: op[1],
                done: false
              };

            case 5:
              _.label++;
              y = op[1];
              op = [0];
              continue;

            case 7:
              op = _.ops.pop();

              _.trys.pop();

              continue;

            default:
              if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
                _ = 0;
                continue;
              }

              if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
                _.label = op[1];
                break;
              }

              if (op[0] === 6 && _.label < t[1]) {
                _.label = t[1];
                t = op;
                break;
              }

              if (t && _.label < t[2]) {
                _.label = t[2];

                _.ops.push(op);

                break;
              }

              if (t[2]) _.ops.pop();

              _.trys.pop();

              continue;
          }

          op = body.call(thisArg, _);
        } catch (e) {
          op = [6, e];
          y = 0;
        } finally {
          f = t = 0;
        }
      }

      if (op[0] & 5) throw op[1];
      return {
        value: op[0] ? op[1] : void 0,
        done: true
      };
    }
  }

  var _assign = function __assign() {
    _assign = Object.assign || function __assign(t) {
      for (var s, i = 1, n = arguments.length; i < n; i++) {
        s = arguments[i];

        for (var p in s) {
          if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
        }
      }

      return t;
    };

    return _assign.apply(this, arguments);
  };
  function __awaiter$1(thisArg, _arguments, P, generator) {
    function adopt(value) {
      return value instanceof P ? value : new P(function (resolve) {
        resolve(value);
      });
    }

    return new (P || (P = Promise))(function (resolve, reject) {
      function fulfilled(value) {
        try {
          step(generator.next(value));
        } catch (e) {
          reject(e);
        }
      }

      function rejected(value) {
        try {
          step(generator["throw"](value));
        } catch (e) {
          reject(e);
        }
      }

      function step(result) {
        result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
      }

      step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
  }
  function __generator$1(thisArg, body) {
    var _ = {
      label: 0,
      sent: function sent() {
        if (t[0] & 1) throw t[1];
        return t[1];
      },
      trys: [],
      ops: []
    },
        f,
        y,
        t,
        g;
    return g = {
      next: verb(0),
      "throw": verb(1),
      "return": verb(2)
    }, typeof Symbol === "function" && (g[Symbol.iterator] = function () {
      return this;
    }), g;

    function verb(n) {
      return function (v) {
        return step([n, v]);
      };
    }

    function step(op) {
      if (f) throw new TypeError("Generator is already executing.");

      while (_) {
        try {
          if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
          if (y = 0, t) op = [op[0] & 2, t.value];

          switch (op[0]) {
            case 0:
            case 1:
              t = op;
              break;

            case 4:
              _.label++;
              return {
                value: op[1],
                done: false
              };

            case 5:
              _.label++;
              y = op[1];
              op = [0];
              continue;

            case 7:
              op = _.ops.pop();

              _.trys.pop();

              continue;

            default:
              if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
                _ = 0;
                continue;
              }

              if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
                _.label = op[1];
                break;
              }

              if (op[0] === 6 && _.label < t[1]) {
                _.label = t[1];
                t = op;
                break;
              }

              if (t && _.label < t[2]) {
                _.label = t[2];

                _.ops.push(op);

                break;
              }

              if (t[2]) _.ops.pop();

              _.trys.pop();

              continue;
          }

          op = body.call(thisArg, _);
        } catch (e) {
          op = [6, e];
          y = 0;
        } finally {
          f = t = 0;
        }
      }

      if (op[0] & 5) throw op[1];
      return {
        value: op[0] ? op[1] : void 0,
        done: true
      };
    }
  }

  /* eslint-disable @typescript-eslint/no-explicit-any */
  var constructObjectsInArray = function constructObjectsInArray(array, objectParser) {
    var arr = [];

    if (!array || !Array.isArray(array)) {
      return arr;
    }

    array.forEach(function (element) {
      arr.push(objectParser(element));
    });
    return arr;
  };

  /* eslint-disable @typescript-eslint/no-explicit-any */
  var normalizeStringToArray = function normalizeStringToArray(stringOrOther) {
    if (typeof stringOrOther === 'string') {
      stringOrOther = [stringOrOther];
    }

    return stringOrOther;
  };

  var Properties =
  /** @class */
  function () {
    function Properties() {}

    Properties.Parse = function (object) {
      var properties = new Properties();
      Object.assign(properties, object);
      return properties;
    };

    return Properties;
  }();

  var Link =
  /** @class */
  function () {
    function Link() {}

    Link.Parse = function (object) {
      var link = new Link(); // Copy all properties over to the new object

      Object.assign(link, object); // Construct Properties

      if (link.properties) {
        link.properties = Properties.Parse(link.properties);
      } // Recursively parse Alternates as Links


      if (link.alternate) {
        link.alternate = constructObjectsInArray(link.alternate, Link.Parse);
      } // Recursively parse children as Links


      if (link.children) {
        link.children = constructObjectsInArray(link.children, Link.Parse);
      }

      if (link.rel) {
        link.rel = normalizeStringToArray(link.rel);
      }

      if (link.language) {
        link.language = normalizeStringToArray(link.language);
      }

      return link;
    };

    return Link;
  }();

  var Contributor =
  /** @class */
  function () {
    function Contributor() {}
    /**
     * @param {object | string | string[] | object[]} contributorDescription
     * string - https://github.com/readium/webpub-manifest/blob/0976680e25852b8a4c4802a052ba750ab3e89284/schema/contributor.schema.json#L7
     * object - https://github.com/readium/webpub-manifest/blob/0976680e25852b8a4c4802a052ba750ab3e89284/schema/contributor.schema.json#L24
     * array of strings - https://github.com/readium/webpub-manifest/blob/0976680e25852b8a4c4802a052ba750ab3e89284/schema/contributor.schema.json#L10
     * array of objects - https://github.com/readium/webpub-manifest/blob/0976680e25852b8a4c4802a052ba750ab3e89284/schema/contributor.schema.json#L17
     * @returns {Contributor[]}
     */


    Contributor.Parse = function (contributorDescription) {
      if (Array.isArray(contributorDescription)) {
        // If json is an array, initialize all elements as Contributor
        return constructObjectsInArray( // eslint-disable-next-line @typescript-eslint/no-explicit-any
        contributorDescription, Contributor.ParseContributorObject);
      } // Otherwise, proceed to parsing the full Contributor Object


      return [this.ParseContributorObject(contributorDescription)];
    };

    Contributor.ParseContributorObject = function (objectOrString) {
      var contributor = new Contributor();

      if (typeof objectOrString === 'string') {
        // If contributor is a simple string
        contributor.name = {
          '': objectOrString
        }; // No more fields to parse

        return contributor;
      }

      Object.assign(contributor, objectOrString); // Parse name as LocalizedString

      if (typeof contributor.name === 'string') {
        contributor.name = {
          '': contributor.name
        };
      } // Parse links as Links


      if (contributor.links) {
        contributor.links = constructObjectsInArray(contributor.links, Link.Parse);
      }

      if (contributor.role) {
        contributor.role = normalizeStringToArray(contributor.role);
      }

      return contributor;
    };

    return Contributor;
  }();

  var Subject =
  /** @class */
  function () {
    function Subject() {}

    Subject.Parse = function (object) {
      if (Array.isArray(object)) {
        // If an array, parse all elements
        return constructObjectsInArray(object, Subject.ParseSubjectObject);
      }

      return [Subject.ParseSubjectObject(object)];
    };

    Subject.ParseSubjectObject = function (object) {
      var subject = new Subject();

      if (typeof object === 'string') {
        // If subject is a simple string
        subject.name = {
          '': object
        }; // No more fields to parse

        return subject;
      } // Otherwise, parse the Subject object


      Object.assign(subject, object); // Parse name as LocalizedString

      if (typeof subject.name === 'string') {
        subject.name = {
          '': subject.name
        };
      }

      if (subject.links) {
        subject.links = constructObjectsInArray(subject.links, Link.Parse);
      }

      return subject;
    };

    return Subject;
  }();

  function supplementFlowpubMetadataWithReadium2FormatMetadata(metadata, metadataSource) {
    var metadataCopy = _assign({}, metadata);

    if (!metadataCopy.presentation && metadataSource.rendition) {
      // In Readium2 specs https://github.com/readium/webpub-manifest/blob/917c83e798e3eda42b3e9d0dc92f0fef31b16211/schema/extensions/epub/metadata.schema.json#L7
      // Flowpub "presentation" field matches the Readium "rendition".
      metadataCopy.presentation = metadataSource.rendition;
    }

    if (!metadataCopy.readingProgression && metadataSource.direction) {
      // According to the code:
      // https://github.com/readium/r2-shared-js/blob/develop/src/models/metadata.ts#L373
      // "readingProgression" might be in the field "direction"
      metadataCopy.readingProgression = metadataSource.direction;
    }

    return metadataCopy;
  }

  var Metadata =
  /** @class */
  function () {
    function Metadata() {}
    /**
     * Object - is a manifest.json:metadata deserialized
     *
     * @static
     * @param {object} object - should conform to JSON schema
     * main - https://github.com/readium/webpub-manifest/blob/master/schema/metadata.schema.json
     * extension - https://github.com/readium/webpub-manifest/blob/917c83e798e3eda42b3e9d0dc92f0fef31b16211/schema/extensions/epub/metadata.schema.json
     * @return {Metadata}
     * @memberof Metadata
     */


    Metadata.Parse = function (object, supplementMetadata) {
      if (supplementMetadata === void 0) {
        supplementMetadata = supplementFlowpubMetadataWithReadium2FormatMetadata;
      }

      var metadata = new Metadata();
      Object.assign(metadata, object);

      if (typeof metadata.title === 'string') {
        metadata.title = {
          '': metadata.title
        };
      }

      if (typeof metadata.subtitle === 'string') {
        metadata.subtitle = {
          '': metadata.subtitle
        };
      }

      if (metadata.subject) {
        metadata.subject = Subject.Parse(metadata.subject);
      } // A list of all Contributor fields


      var contributorProperties = ['artist', 'author', 'translator', 'editor', 'illustrator', 'letterer', 'penciler', 'colorist', 'inker', 'narrator', 'contributor', 'publisher', 'imprint'];
      contributorProperties.forEach(function (element) {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        var localMetadata = metadata;

        if (localMetadata[element]) {
          localMetadata[element] = Contributor.Parse(localMetadata[element]);
        }
      }); // constructObjects( metadata, contributorProperties, Contributor.Parse );

      if (metadata.published) {
        metadata.published = new Date(metadata.published);
      }

      if (metadata.modified) {
        metadata.modified = new Date(metadata.modified);
      }

      if (metadata.language) {
        metadata.language = normalizeStringToArray(metadata.language);
      }

      if (metadata.belongsTo) {
        var belongsTo = metadata.belongsTo;

        if (belongsTo.collection) {
          belongsTo.collection = Contributor.Parse(belongsTo.collection);
        }

        if (belongsTo.series) {
          belongsTo.series = Contributor.Parse(belongsTo.series);
        }
      }

      return supplementMetadata(metadata, object);
    };

    return Metadata;
  }();

  var Publication =
  /** @class */
  function () {
    function Publication() {}

    Publication.prototype.getAsAbsoluteURL = function (href) {
      var newHref = href;

      if (newHref[0] === '/') {
        newHref = newHref.slice(1);
      }

      return new URL(newHref, this.selfLink.href);
    };

    Publication.prototype.getBaseURI = function () {
      // NOTE I'm not sure if we will have SourceURI ever
      // if( this.sourceURI ) {
      //     href = new URL('./', this.sourceURI).toString();
      // }
      // NOTE The following produces an error
      // return new URL('./', selfLink.href).toString();
      if (!this.baseURI) {
        this.baseURI = this.selfLink.href.slice(0, this.selfLink.href.lastIndexOf('/') + 1);
      } // TODO Should not be using a base URI at all


      return this.baseURI;
    };

    Object.defineProperty(Publication.prototype, "spine", {
      get: function get() {
        return this.readingOrder;
      },
      enumerable: false,
      configurable: true
    });

    Publication.prototype.getCover = function () {
      if (this.resources) {
        return Publication.SearchForLinkInCollectionByRel(this.resources, 'cover');
      }
    };

    Publication.prototype.searchLinkByRel = function (rel) {
      if (this.readingOrder) {
        var target = Publication.SearchForLinkInCollectionByRel(this.readingOrder, rel);

        if (target) {
          return target;
        }
      }

      if (this.resources) {
        var target = Publication.SearchForLinkInCollectionByRel(this.resources, rel);

        if (target) {
          return target;
        }
      }

      if (this.links) {
        var target = Publication.SearchForLinkInCollectionByRel(this.links, rel);

        if (target) {
          return target;
        }
      }

      return undefined;
    };

    Publication.prototype.findSpineItemIndexByHref = function (href) {
      return this.readingOrder.findIndex(function (element) {
        return element.href === href;
      });
    };

    Publication.SearchForLinkInCollectionByRel = function (collection, rel) {
      return collection.find(function (element) {
        return element.rel && element.rel.includes(rel);
      });
    };
    /**
     * Parse a manifest into a publication.
     * @param {Object} object JSON-like object
     * @param {string} [baseURL=null] Optional base URL (self link href)
     * @throws {Error} Argument object must be an object.
     */


    Publication.Parse = function (object, baseURL) {
      if (baseURL === void 0) {
        baseURL = null;
      }

      return __awaiter$1(this, void 0, void 0, function () {
        var publication, manifest, selfLink, _a, out;

        return __generator$1(this, function (_b) {
          switch (_b.label) {
            case 0:
              publication = new Publication();
              /* NOTE The definitions of which property are Extensions are unclear
               *  I'm unclear as to why the following was dictated [Context, Metadata, Links, ReadingOrder, Resources] to
               *  to be in Core, and everything else in EPUB Extension
               *  https://github.com/evidentpoint/r2-webpub-model-js/blob/master/src/models/publication/publication.ts
               *  -
               *  whereas the following has very different roles for them
               *  https://readium.org/webpub-manifest/roles.html
               */

              if (_typeof(object) !== 'object') {
                throw new Error('Argument object must be an object.');
              }

              manifest = Object.assign({}, object); // @Context

              if (manifest['@context']) {
                publication['@context'] = manifest['@context'];
              }

              delete manifest['@context']; // Metadata, Required

              publication.metadata = Metadata.Parse(manifest.metadata);
              delete manifest.metadata; // Links, Required(could be null)

              publication.links = constructObjectsInArray(manifest.links, Link.Parse);
              delete manifest.links;
              selfLink = this.SearchForLinkInCollectionByRel(publication.links, 'self');

              if (!selfLink) {
                selfLink = new Link();
                selfLink.rel = ['self'];
              } // Override the 'self' link's href if there is a baseURL


              if (baseURL) {
                selfLink.href = baseURL;
              }

              if (!selfLink.href) {
                throw new Error('Miss self link or base url.');
              }

              if (manifest['fp:blobData']) {
                publication.blobData = manifest['fp:blobData'];
              }

              publication.selfLink = selfLink; // Then if there is no 'self' link in the manifest, push our baseURL link into the model

              if (!this.SearchForLinkInCollectionByRel(publication.links, 'self')) {
                publication.links.push(publication.selfLink);
              } // Reading Order, Required


              publication.readingOrder = constructObjectsInArray(manifest.readingOrder, Link.Parse);
              delete manifest.readingOrder; // Resources

              if (manifest.resources) {
                publication.resources = constructObjectsInArray(manifest.resources, Link.Parse);
              }

              delete manifest.resources;
              if (!manifest.toc) return [3
              /*break*/
              , 1];
              publication.toc = constructObjectsInArray(manifest.toc, Link.Parse);
              return [3
              /*break*/
              , 3];

            case 1:
              // eslint-disable-next-line @typescript-eslint/no-use-before-define
              _a = publication;
              return [4
              /*yield*/
              , defineToc(publication)];

            case 2:
              // eslint-disable-next-line @typescript-eslint/no-use-before-define
              _a.toc = _b.sent();
              _b.label = 3;

            case 3:
              delete manifest.toc;
              out = parseAsSubcollections(manifest);
              Object.assign(publication, out);
              return [2
              /*return*/
              , publication];
          }
        });
      });
    };

    return Publication;
  }();

  var parseAsSubcollectionObject = function parseAsSubcollectionObject(json) {
    var subcollectionJSON = Object.assign({}, json); // eslint-disable-next-line @typescript-eslint/no-explicit-any

    var subcollection = {};

    if (!subcollectionJSON.metadata || !subcollectionJSON.links) {
      return subcollectionJSON;
    }

    subcollection.metadata = subcollectionJSON.metadata;
    delete subcollectionJSON.metadata;
    subcollection.links = constructObjectsInArray(subcollectionJSON.links, Link.Parse);
    delete subcollectionJSON.links; // eslint-disable-next-line @typescript-eslint/no-use-before-define

    Object.assign(subcollection, parseAsSubcollections(subcollectionJSON));
    return subcollection;
  }; // eslint-disable-next-line @typescript-eslint/no-explicit-any


  var parseAsSubcollections = function parseAsSubcollections(json) {
    var otherProperties = Object.assign({}, json); // eslint-disable-next-line @typescript-eslint/no-explicit-any

    var otherCollections = {}; // All others treated as a sub-collection

    Object.keys(otherProperties).forEach(function (property) {
      // Set the sub collection
      var collection = otherProperties[property];

      if (Array.isArray(collection)) {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        var newCollection_1 = []; // If it's an array, iterate through the array

        collection.forEach(function (element) {
          // Figure out if it's a Link or not
          if (element.href) {
            // Treat as Link
            newCollection_1.push(Link.Parse(element));
          } else {
            // Treat as a Sub Collection object
            newCollection_1.push(parseAsSubcollectionObject(element));
          }
        }); // Replace the original property with the Sub Collection

        otherCollections[property] = newCollection_1;
      } else {
        // If not an Array, treat as a Sub Collection Object
        otherCollections[property] = parseAsSubcollectionObject(collection);
      }
    });
    return otherCollections;
  }; // Find the entry for contents if it exists


  var findContentsInCollections = function findContentsInCollections(collections) {
    for (var _i = 0, collections_1 = collections; _i < collections_1.length; _i++) {
      var collection = collections_1[_i];

      if (collection) {
        var link = Publication.SearchForLinkInCollectionByRel(collection, 'contents');

        if (link) {
          return link;
        }
      }
    }

    return undefined;
  };

  var defineToc = function defineToc(publication) {
    return __awaiter$1(void 0, void 0, void 0, function () {
      var cols, contents, fetchDoc, parser, doc, tocElement, nodes;

      var _a;

      return __generator$1(this, function (_b) {
        switch (_b.label) {
          case 0:
            // TODO Guard against non-browser environment for now
            // eslint-disable-next-line no-undef
            if (typeof window == 'undefined') {
              // NOT IN BROWSER
              console.error('Not in browser environment');
              return [2
              /*return*/
              , undefined];
            }

            cols = [publication.readingOrder];

            if (publication.resources) {
              cols.push(publication.resources);
            }

            contents = findContentsInCollections(cols);

            if (!contents) {
              // NOTE Really should throw an error or something
              console.error('Could not find Contents entry in manifest');
              return [2
              /*return*/
              , undefined];
            }

            return [4
            /*yield*/
            , fetch(publication.getAsAbsoluteURL(contents.href).href)];

          case 1:
            return [4
            /*yield*/
            , _b.sent().text() // Get the ToC Entry
            ];

          case 2:
            fetchDoc = _b.sent();
            parser = new DOMParser();
            doc = parser.parseFromString(fetchDoc, 'text/html');
            tocElement = doc.querySelector('[epub\\:type="toc"]');
            nodes = (_a = tocElement === null || tocElement === void 0 ? void 0 : tocElement.lastElementChild) === null || _a === void 0 ? void 0 : _a.children; // eslint-disable-next-line @typescript-eslint/no-use-before-define

            return [2
            /*return*/
            , parseHTMLCollectionAsLinks(nodes)];
        }
      });
    });
  }; // Parse node as a link object
  // eslint-disable-next-line @typescript-eslint/no-explicit-any


  var parseHTMLCollectionAsLinks = function parseHTMLCollectionAsLinks(nodes) {
    var result = []; // https://stackoverflow.com/questions/22754315/for-loop-for-htmlcollection-elements

    for (var i = 0; i < nodes.length; i++) {
      // For each <li> node, get its children
      var nodeItems = nodes.item(i).children; // eslint-disable-next-line @typescript-eslint/no-explicit-any

      var output = {}; // The first element should always be a <span> or <a>

      var item = nodeItems.item(0); // The title is the text of the tag

      output.title = item.innerHTML; // Get the HREF if it's an anchor, or TODO

      if (item.tagName.toLocaleLowerCase() === 'a') {
        output.href = item.getAttribute('href');
      } else {
        // TODO Tag is SPAN, so no link should be present
        output.href = '#';
      } // Check for children and recursively parse


      if (nodeItems.length > 1) {
        output.children = parseHTMLCollectionAsLinks(nodeItems.item(1).children);
      } // Push the result parsed into a Link


      result.push(Link.Parse(output));
    }

    return result;
  };

  function parse(manifest, selfURL) {
    return __awaiter(this, void 0, void 0, function () {
      return __generator(this, function (_a) {
        return [2
        /*return*/
        , Publication.Parse(manifest, selfURL)];
      });
    });
  }
  function parseFromJSON(string, selfURL) {
    return __awaiter(this, void 0, void 0, function () {
      var manifest;
      return __generator(this, function (_a) {
        manifest = JSON.parse(string);
        return [2
        /*return*/
        , parse(manifest, selfURL)];
      });
    });
  }
  function parseFromURL(url, selfURL) {
    return __awaiter(this, void 0, void 0, function () {
      var response, manifest;
      return __generator(this, function (_a) {
        switch (_a.label) {
          case 0:
            return [4
            /*yield*/
            , fetch(url)];

          case 1:
            response = _a.sent();
            return [4
            /*yield*/
            , response.json()];

          case 2:
            manifest = _a.sent();
            return [2
            /*return*/
            , parse(manifest, selfURL)];
        }
      });
    });
  }

  exports.parse = parse;
  exports.parseFromJSON = parseFromJSON;
  exports.parseFromURL = parseFromURL;

  Object.defineProperty(exports, '__esModule', { value: true });

})));
//# sourceMappingURL=bundle.js.map
